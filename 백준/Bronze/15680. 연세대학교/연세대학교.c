#include <stdio.h>
#include <string.h>
#pragma warning(disable:4996)

int main() {
    int a;
    scanf("%d", &a);
    if(a==0)
        printf("YONSEI");
    else if(a==1)
        printf("Leading the Way to the Future");
    return 0;
}