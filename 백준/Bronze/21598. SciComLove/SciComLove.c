#include <stdio.h>
#include <string.h>
#pragma warning(disable:4996)

int main()
{
    int n;
    scanf("%d", &n);
    for(int i=0;i<n;i++)
        printf("SciComLove\n");
    return (0);
}